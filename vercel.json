{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "npx convex deploy --cmd 'next build'",
  "devCommand": "npx convex dev --once && next dev",
  "installCommand": "npm install"
}
