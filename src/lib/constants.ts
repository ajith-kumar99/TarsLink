// App-wide constants

export const PRESENCE_HEARTBEAT_INTERVAL_MS = 20_000; // 20 seconds
export const PRESENCE_TIMEOUT_MS = 30_000;            // 30 seconds — user is offline after this
export const TYPING_TIMEOUT_MS = 2_000;               // 2 seconds — typing indicator disappears
export const MESSAGE_PAGE_SIZE = 50;                   // messages per page
